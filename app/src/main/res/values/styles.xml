<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- styles for tab layout -->
    <style name="TruSpotTabLayout" parent="Widget.Design.TabLayout">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="tabTextAppearance">@style/TruSpotTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/icons</item>
    </style>

    <style name="TruSpotTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">#80FFFFFF</item>
    </style>

    <!-- styles for fab -->
    <style name="FAB" parent="Material.Widget.FloatingActionButton.Light">
        <item name="rd_rippleColor">#33000000</item>
        <item name="fab_backgroundColor">@color/icons</item>
    </style>

</resources>
